#include <stdio.h>

// oops, made a mistake. forgot to include the x as an int //

int main(void)
{
    int x = 1;

if (x > 5) // Finally removed the semicolon which was def like my ex gf, manipulative asf. Atleast removing the semicolon should do the job //
// In this exact line, i forgot to remove the "{" bracket, which was causing gcc to freak out, atleast now i learned what exactly was the error, now corrected //
    printf("x is great\n"); // Lmao forgot to include semicolon, atleast now i did //
    return 0; // Damn forgot this one too, atleast i remembered now //
}
